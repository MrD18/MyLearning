<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yss.mapper.LayerMapper">
  <insert id="insert" parameterType="com.yss.dto.Layer">

 insert into layer (layerId, layerName, description,
      releaseTime, recordTime)
    values (#{layerId,jdbcType=VARCHAR}, #{layerName,jdbcType=VARCHAR}, #{description,jdbcType=INTEGER},
      #{releaseTime,jdbcType=TIMESTAMP}, #{recordTime,jdbcType=TIMESTAMP}
      )
  </insert>

    <select id="getList" resultType="com.yss.dto.Layer">
        select * from layer
    </select>

    <select id="getList2" resultType="com.yss.dto.Layer2">
        select * from layer
    </select>




    <insert id="insert2" parameterType="com.yss.dto.Layer2" useGeneratedKeys="true" keyProperty="layerId" >
        insert into layer (layerId, layerName, description,
      releaseTime, recordTime)
    values (#{layerId,jdbcType=VARCHAR}, #{layerName,jdbcType=VARCHAR}, #{description,jdbcType=INTEGER},
      #{releaseTime,jdbcType=TIMESTAMP}, #{recordTime,jdbcType=TIMESTAMP}
      )
    </insert>

</mapper>